class Book {
  constructor(name, description, amount, isAvailable, genre) {
    this.bookName = name;
    this.bookDescription = description;
    this.bookAmount = amount;
    this.bookIsAvailable = isAvailable;
    this.bookGenre = genre;
  }
}
